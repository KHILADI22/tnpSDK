apply plugin: 'com.android.library'

android {
    compileSdkVersion 31 // Use your desired SDK version
    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 31 // Use your desired SDK version
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation 'com.facebook.react:react-native:+'
}